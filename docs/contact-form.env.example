# Contact form (Resend) — copy values into `.env.local` (never commit secrets).
# This file is not named `.env*` so it can live in Git.

RESEND_API_KEY=
# Example for Resend trial: Ground Level Website <onboarding@resend.dev>
RESEND_FROM=
CONTACT_NOTIFY_EMAIL=
