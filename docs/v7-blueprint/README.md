# V7 blueprint references

Archived **reference implementations** extracted from the legacy monolithic `master/MASTER_RULES.md` (V7 architectural truth). These files are **not** imported by the app by default; they document the canonical pattern for agents and humans.

- **`GLHeroMetricsCoverageBlock.reference.tsx`** — dark glass hero metrics + coverage chips (tri-color typography per `design-system.mdc` §8).

Authoritative rules: [`.cursor/rules/design-system.mdc`](../../.cursor/rules/design-system.mdc).
